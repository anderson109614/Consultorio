CREATE TABLE EXAMENES(
    COD_EXA VARCHAR(3) NOT NULL PRIMARY KEY,
    TIP_EXA VARCHAR(20) NOT NULL,
    COS_EXA NUMBER NOT NULL,
    );

ALTER TABLE LABORATORISTAS
ADD PA_LAB VARCHAR(10) NOT NULL;
1803760154

CREATE TABLE CONSULTAS
(
COD_CON VARCHAR(5) PRIMARY KEY,
CI_CLI VARCHAR(10) REFERENCES CLIENTES(CED_CLI),
CI_LAB VARCHAR(10) REFERENCES LABORATORISTAS(CI_LAB),
FEC_CON DATE NOT NULL,
FEC_RET DATE
);

CREATE TABLE DETALLES
(
COD_CON VARCHAR(5) REFERENCES CONSULTAS(COD_CON),
COD_EXA VARCHAR(3) REFERENCES EXAMENES(COD_EXA),
RES_EXA VARCHAR(20),
FEC_RES DATE
);